{
  "name": "mason-report",
  "version": "0.2.0",
  "private": true,
  "description": "Local, hooks-based observability plugin for Claude Code. Reconstructs how Claude handled a request from observed hook/transcript evidence only, separating observed facts, inferred interpretation, and unknowns.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "validate": "node tests/validate.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fe-hyunsu/mason-report"
  }
}
